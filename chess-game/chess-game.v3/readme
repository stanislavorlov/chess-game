To run locally run the following command:

cd web

Back-end API
- fastapi dev main.py

//uvicorn app:app --reload

sudo lsof -t -i tcp:8000 | xargs kill -9

Client app


Chess-engine
Chess-application