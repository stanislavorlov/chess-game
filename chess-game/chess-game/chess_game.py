from Model.Pieces.Pawn import Pawn
from Model.State.GameState import GameState
from Model.ValueObjects.Color import Color

# game_state = GameState()

print(Color.WHITE())
print(Color.BLACK())

pawn1 = Pawn(id=f"wp_{file}", color=Color.WHITE), f"{file}2")
